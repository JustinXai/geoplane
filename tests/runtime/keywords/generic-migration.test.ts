import {readFile} from "node:fs/promises";import {dirname,join} from "node:path";import {fileURLToPath} from "node:url";import {describe,expect,it} from "vitest";
const path=join(dirname(fileURLToPath(import.meta.url)),"..","..","..","migrations","0018_generic_keyword_core.sql");
describe("0018 generic keyword core migration",()=>{it("is additive, source-neutral, and separates demand evidence",async()=>{const sql=await readFile(path,"utf8");expect(sql).toContain("CREATE TABLE keyword_dataset");expect(sql).toContain("CREATE TABLE keyword_record");expect(sql).toContain("CREATE TABLE demand_evidence");for(const source of ["MANUAL","GENERIC_FILE","BAIDU_KEYWORD","CUSTOMER_HISTORY","OTHER_PROVIDER"])expect(sql).toContain(source);expect(sql).not.toContain("ALTER TABLE keyword_reference_source_import");expect(sql).not.toContain("CONFIRMED_DEMAND");});});
