import {describe,expect,it} from "vitest";import {evaluateVerticalPolicyPack,nextGateAfterVertical} from "../../../src/runtime/policy-packs/evaluator.js";import {MEDICAL_AESTHETICS_V1,createDomesticP0PolicyPackRegistry} from "../../../src/runtime/policy-packs/medical-aesthetics-v1.js";
describe("MEDICAL_AESTHETICS_V1 minimum evidence-bound pack",()=>{it("contains only the eight frozen categories and no invented term vocabulary",()=>{expect(MEDICAL_AESTHETICS_V1.rules.map(r=>r.category)).toEqual(["MEDICAL_ADVERTISING_BOUNDARY","ABSOLUTE_WORDING","EFFICACY_COMMITMENT","PRACTITIONER_QUALIFICATION","INSTITUTION_QUALIFICATION","PRODUCT_QUALIFICATION","RISK_TERM_REVIEW","EVIDENCE_REQUIREMENT"]);expect(MEDICAL_AESTHETICS_V1.riskTerms).toEqual([]);expect(MEDICAL_AESTHETICS_V1.mandatoryHumanReview).toBe(true);expect(createDomesticP0PolicyPackRegistry().get("MEDICAL_AESTHETICS_V1",1)).toBeTruthy();});it("fails closed without explicit human confirmations",()=>{const result=evaluateVerticalPolicyPack(MEDICAL_AESTHETICS_V1,{projectId:"p",industryProfileId:"i",qualityGateStatus:"PASSED",platformGateStatus:"PASSED"});expect(result.status).toBe("FAILED");expect(result.failureReasons).toHaveLength(8);expect(nextGateAfterVertical(result)).toBe("BLOCKED");});it("passes the vertical gate only with a named reviewer and every category confirmed, then still requires human review",()=>{const result=evaluateVerticalPolicyPack(MEDICAL_AESTHETICS_V1,{projectId:"p",industryProfileId:"i",qualityGateStatus:"PASSED",platformGateStatus:"PASSED",manualReviewerId:"reviewer-1",manuallyConfirmedRuleIds:MEDICAL_AESTHETICS_V1.rules.map(r=>r.ruleId)});expect(result.status).toBe("PASSED");expect(result.requiresHumanReview).toBe(true);expect(nextGateAfterVertical(result)).toBe("HUMAN_REVIEW");});});
