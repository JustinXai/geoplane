import {apiErr,apiOk} from "../../../../../runtime/api-contracts/index.js";import {toHttpResponse} from "../../../../../runtime/auth/http.js";import {genericKeywordContext,invalid} from "../../../../../runtime/generic-keywords/http.js";import {readGenericKeywordWorkspace} from "../../../../../runtime/generic-keywords/read-model.js";
export const runtime="nodejs";export const dynamic="force-dynamic";
export async function GET(request:Request,context:{params:Promise<{datasetId:string}>}){const {datasetId}=await context.params,projectId=new URL(request.url).searchParams.get("projectId")?.trim();if(!projectId)return invalid("缺少项目参数。");const ctx=await genericKeywordContext(request,projectId);if("response" in ctx)return ctx.response;const data=await readGenericKeywordWorkspace(ctx.value.runtime.db,{clientOrganizationId:ctx.value.project.clientOrganizationId,projectId},{datasetId});if(data.datasets.length===0)return toHttpResponse(apiErr("NOT_FOUND","关键词数据集不存在。"));return toHttpResponse(apiOk(data));}
