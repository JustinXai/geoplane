/**
 * Recovery classification: RECONSTRUCTED_FROM_FROZEN_SPEC
 * reconstruction_reason: package.json/tsconfig/source-root were not
 * recoverable (see docs/rebuild/RECOVERY_GAP_ANALYSIS.md, P0 gap).
 * original_file_unavailable: true
 *
 * Placeholder entry point for the minimal P0 scaffold. Real application
 * code lands per-lane in rebuild/tenancy-auth, rebuild/frontend-workspaces,
 * rebuild/geo-business-pipeline.
 */
export const SCAFFOLD_VERSION = "P0_PROJECT_SCAFFOLD_V1";
