import type {AgencyWorkflowEvent,AgencyWorkflowStage,AgencyWorkflowStatus} from "./contracts.js";
const allowed:Record<AgencyWorkflowStatus,readonly AgencyWorkflowStatus[]>={NOT_STARTED:["IN_PROGRESS"],IN_PROGRESS:["WAITING_CLIENT","BLOCKED","COMPLETED"],WAITING_CLIENT:["IN_PROGRESS","BLOCKED"],BLOCKED:["IN_PROGRESS"],COMPLETED:[]};
export function transitionAgencyWorkflow(input:Omit<AgencyWorkflowEvent,"fromStatus"|"toStatus">&{fromStatus:AgencyWorkflowStatus;toStatus:AgencyWorkflowStatus}):AgencyWorkflowEvent{if(!allowed[input.fromStatus].includes(input.toStatus))throw new Error(`INVALID_AGENCY_WORKFLOW_TRANSITION:${input.fromStatus}->${input.toStatus}`);if(!input.actorUserId.trim())throw new Error("WORKFLOW_ACTOR_REQUIRED");if(!input.reason.trim())throw new Error("WORKFLOW_REASON_REQUIRED");return input;}
export function firstIncompleteStage(statuses:ReadonlyMap<AgencyWorkflowStage,AgencyWorkflowStatus>,order:readonly AgencyWorkflowStage[]):AgencyWorkflowStage{return order.find(stage=>statuses.get(stage)!=="COMPLETED")??order[order.length-1]!;}
