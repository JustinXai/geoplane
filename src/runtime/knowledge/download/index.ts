/**
 * Recovery classification: RECONSTRUCTED_FROM_FROZEN_SPEC (net-new runtime-phase file)
 * reconstruction_source: ./url-fetcher.ts.
 * reconstruction_reason: KNOWLEDGE_INGESTION_RELIABILITY_V1 - single public entry point for the
 *   URL download helper that feeds bytes+contentType into the reliable parser.
 * original_file_unavailable: n/a (net-new runtime-phase file)
 */
export {
  DEFAULT_URL_TIMEOUT_MS,
  fetchUrlDocument,
  type FetchLike,
  type FetchLikeResponse,
  type FetchUrlDocumentInput,
  type FetchedUrlDocument,
} from "./url-fetcher.js";
