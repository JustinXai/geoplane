/**
 * Recovery classification: RECONSTRUCTED_FROM_FROZEN_SPEC
 * reconstruction_source: docs/rebuild/RECOVERY_GAP_ANALYSIS.md (P0 gap: package.json/tsconfig/
 *   source root not recovered)
 * reconstruction_reason: no original file recoverable
 * original_file_unavailable: true
 *
 * Minimal Next.js config for the C1 "shell" checkpoint. Deliberately empty -
 * no experimental flags, no rewrites/redirects, no server config, no
 * database or datasource wiring of any kind.
 */
import type { NextConfig } from "next";

const nextConfig: NextConfig = {};

export default nextConfig;
