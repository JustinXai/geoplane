# DAILY_DELIVERY_BOARD

Overnight loop: `GEO_CONTROL_PLANE_OVERNIGHT_PARALLEL_REBUILD_V1`, started 2026-07-18, session-only cron (job `e006b6fd`, every 30 min, expires when this Claude session ends or after 7 days).

| Time (local) | Lane | Checkpoint | Branch | Commit SHA | Status | Notes |
|---|---|---|---|---|---|---|
| 2026-07-18 01:3x | B | B1 — core tenancy contracts | `rebuild/tenancy-auth` | _pending push_ | IN_PROGRESS | `src/contracts/tenancy/entities.ts` — type-level only, all RECONSTRUCTED_FROM_FROZEN_SPEC. No package.json/tsconfig yet (P0 gap), so not typechecked this cycle — recorded as `DEPENDENCY_RESTORE_REQUIRED`, not faked as PASS. |
| 2026-07-18 02:1x | B | B2 — authorization rules | `rebuild/tenancy-auth` | `a4cc84027e72f07b0014fb9832fd1defdfc1e0c5` | PASS (typecheck PASS / test PASS) | `src/contracts/tenancy/authorization.ts` — runnable `canAccessClientOrganization`/`assertCanAccessClientOrganization` (+ `AuthorizationDeniedError`), RECONSTRUCTED_FROM_FROZEN_SPEC. `tests/contracts/tenancy-authorization.test.ts` covers cross-client denial, own-org access, unassigned/revoked agency denial, active-assignment access, platform-admin access, and no-caller-supplied-org-id coercion. `npm run typecheck` PASS, `npm test` 21/21 PASS (3 files), `node scripts/security-scan.mjs` clean. Pushed; local HEAD == remote HEAD verified via `git rev-parse`/`git ls-remote`. |
| 2026-07-18 02:5x | B | B3 — database schema & migration contract | `rebuild/tenancy-auth` | `c813dcadb36ddba59d3b9f6afe48a78e999cd158` | PASS (typecheck PASS / test PASS) — migration itself is UNTESTED_AGAINST_LIVE_DB | `migrations/0001_tenancy_foundation.sql` — PostgreSQL DDL for all 11 entities in `src/contracts/tenancy/entities.ts`, RECONSTRUCTED_FROM_FROZEN_SPEC. Real FKs for every tenant relationship; `Organization.displayName` deliberately left non-unique (not-blank CHECK only) with the real UNIQUE constraint on `Organization.idempotencyKey`; the CLIENT-single-active-org invariant enforced via a partial unique index on `membership(user_id) WHERE status='ACTIVE' AND organization_type='CLIENT'`, backed by trigger-synced denormalized `organization_type` (see `docs/rebuild/DATABASE_SCHEMA_NOTES.md` for full reasoning); `artifact_index` made DB-level append-only via BEFORE UPDATE/DELETE triggers. No seed data. `npm run typecheck` PASS, `npm test` 21/21 PASS (unchanged, no new TS this checkpoint), `node scripts/security-scan.mjs` clean. **Migration SQL is UNTESTED_AGAINST_LIVE_DB — no database connection is available or permitted in this environment; reviewed by eye only (types, FK direction/ordering, trigger/constraint syntax), not executed against Postgres.** Pushed; local HEAD == remote HEAD verified via `git rev-parse`/`git ls-remote`. |
| 2026-07-18 03:2x | B | B4 — invitation / session / audit logic | `rebuild/tenancy-auth` | `63d518543abb7e173c70f40d14a0b8ba422572cd` | PASS (typecheck PASS / test PASS) | `src/contracts/tenancy/invitations.ts` — `issueInvitation` (raw token returned only as a sibling field, never on the persistable `Invitation` record, which carries `tokenHash` only), `revokeInvitation` (mirrors the recovered `tenancyRepository.revokeInvitation({ invitationId, actor, now })` shape from `recovered/partial-source/00040000000C9C455B787075-route.ts`, gated by an ops-surface check reconstructed as platform-admin-only per B2's `assertIsPlatformAdmin` — no non-platform-admin role, including a CLIENT_OWNER acting on their own org, can revoke), `effectiveInvitationStatus`/`isInvitationUsable` (a PENDING row past `expiresAt` is treated as expired without trusting the stored `status` alone). `src/contracts/tenancy/sessions.ts` — `issueSession` (snapshots role/activeClientOrganizationId/activeProjectId from a resolved `AuthorizationContext`), `isSessionVersionCurrent`/`isSessionValid` (rejects a session snapshotted at a lower `sessionVersion` than the membership's live value, via a new `CurrentMembershipSessionVersion` carrier since B1's `Membership` has no live sessionVersion field of its own — documented as a deliberate B4-local addition, not a redefinition of `Membership`). `src/contracts/tenancy/audit.ts` — `recordAuditEvent`/`computeAuditEventHash` (SHA-256 of a stable JSON stringification of exactly the five fields entities.ts documents as the eventHash input: action, targetType, targetId, actorUserId, createdAt; explicitly documented as illustrative/deterministic-for-tests, not cryptographic audit tooling), corroborated by `recovered/partial-source/00040000000C9C6422CCAB4F-page.tsx`'s `listAudit()`-rendering page reading exactly those fields off each event. All three files RECONSTRUCTED_FROM_FROZEN_SPEC. Added `@types/node` devDependency (needed for `node:crypto` typings under `tsc --noEmit`). Tests: `tests/contracts/tenancy-{invitations,sessions,audit}.test.ts` — raw token never present on/inside the issued `Invitation` record, revoke denied for a non-ops-authorized actor (`AuthorizationDeniedError`), expired-but-PENDING-status invitation correctly unusable, a session issued at `sessionVersion=1` rejected once compared against a bumped current version of 2, and audit hash stability/sensitivity (same inputs -> same hash, any of action/targetType/targetId/actorUserId/createdAt changed -> different hash, metadata changes -> hash unchanged since metadata isn't part of the documented hash input). `npm run typecheck` PASS, `npm test` 43/43 PASS (6 files), `node scripts/security-scan.mjs` clean (run after `git add` of the intended files, since the scanner reads `git ls-files`/the index, not the working tree). Pushed; local HEAD == remote HEAD verified via `git rev-parse`/`git ls-remote`. |
