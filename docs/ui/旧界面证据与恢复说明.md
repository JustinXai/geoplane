# 旧界面证据与恢复说明

## 调查边界

本次只读调查覆盖恢复源 `E:/GEO_RECOVERY_SAFE`、当前仓库 `recovered/partial-source`、`src/app`、`src/components`、`src/styles`、`public`，以及架构、运行时、试点与本地暂存文档。恢复源未被修改。

## 可用证据

| 证据 | 内容 | 判定 |
| --- | --- | --- |
| `000400000009FE5D3E3C4D93/shared.tsx` | Foundation 页面骨架，出现 `shell`、`topbar`、`band`、`panel` | `RECOVERED_EXACT` |
| `00040000000C9C455B787075/route.ts` | 撤销邀请的授权、仓储调用与响应方式 | `RECOVERED_EXACT` |
| `00040000000C9C5B63B4983F/page.tsx` | “组织 / 创建代理商”标题与说明 | `RECOVERED_EXACT` |
| `00040000000C9C5E6C12D671/page.tsx` | “创建客户与项目”及组合表单结构 | `RECOVERED_EXACT` |
| `00040000000C9C607C9A7F12/page.tsx` | “代理商客户分配”及有效分配边界 | `RECOVERED_EXACT` |
| `00040000000C9C6422CCAB4F/page.tsx` | 账户活动表格字段与中文标题 | `RECOVERED_EXACT` |
| `00040000000C9C661E8C211C/page.tsx` | 代理商创建客户与项目页面文案 | `RECOVERED_EXACT` |

上述 7 份可读片段在当前仓库 `recovered/partial-source` 中有副本。恢复源另有 3 份完全由 NUL 字节组成的文件（两个 route、一个 page），没有可解释代码或界面信息，判定为 `RECOVERED_PARTIAL`，不可用于声称准确恢复。

## 可确认的旧界面事实

- 旧界面至少使用中文业务标题、标题上方的业务分组词和一句简短说明。
- 页面曾使用统一标题类 `cp-page-header`、表格容器 `cp-table-wrap`、表格类 `cp-data-table`。
- 已存在组织、客户/项目、代理商客户分配、账户活动等运营页面概念。
- 已存在真实授权与仓储调用，不应把这些能力改造成纯前端演示。

## 不能确认的事项

无法从可用证据准确还原完整侧栏、顶部栏、设计 Token、全量导航顺序、完整 CSS、图标集或全部三角色页面。当前统一布局、导航组织和视觉系统因此标记为 `FROZEN_BUSINESS_STRUCTURE` 与 `REBUILT_PRESENTATION`，不是“原界面复刻”。

## 恢复策略

- 可读片段中的业务名称、页面职责与真实数据边界：保留为 `RECOVERED_EXACT`。
- P0 已验证的后端能力、权限和状态机：作为真实运行时依据，不属于 UI 恢复证据分类。
- 用户指定的三角色信息架构与导航：标记为 `FROZEN_BUSINESS_STRUCTURE`。
- 为达到一致中文企业级体验而新增的组件、Token、空错加载反馈：标记为 `REBUILT_PRESENTATION`。
