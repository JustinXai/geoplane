# 数据资产与恢复来源清单

## 1. 来源优先级

| 来源 | 可用性 | 证据等级与说明 |
|---|---|---|
| `E:\GEO_RECOVERY_SAFE` | AVAILABLE / 永久只读 | 最高恢复证据。包含恢复矩阵、目标状态核验、损坏对象库、孤立源码和两个重建阶段 Bundle。 |
| `recovered/partial-source` | AVAILABLE | 7 个路径丢失但内容可读的部分恢复源码；可证明组织、分配、审计、邀请等具体能力。 |
| 当前 `docs/architecture` | AVAILABLE | 明确标注为项目所有者在灾难重建时重述的冻结规格，不是恢复源码。 |
| 当前 `migrations/0001..0009` | AVAILABLE | 当前重建数据库事实，可证明 `REBUILT_EQUIVALENT`，不能倒推为删除前实现。 |
| 当前测试与运行报告 | AVAILABLE | 当前重建验收事实；安全基线为 953 tests PASS、1 个真实 Provider 微金丝雀跳过。 |
| `docs/DECISIONS.md` | SOURCE_NOT_AVAILABLE | 安全基线不存在。 |
| `docs/ARCHITECTURE.md` | SOURCE_NOT_AVAILABLE | 安全基线不存在；不能用 `docs/architecture/*` 冒充同名历史文档。 |
| `docs/DATA_MODEL.md` | SOURCE_NOT_AVAILABLE | 安全基线不存在。 |
| `docs/ROADMAP_AND_GATES.md` | SOURCE_NOT_AVAILABLE | 安全基线不存在。 |
| `docs/EXECUTION_LEDGER.md` | SOURCE_NOT_AVAILABLE | 安全基线不存在。 |
| `.governor` | SOURCE_NOT_AVAILABLE | 安全基线不存在。 |
| `D:\Documents\geo-reference-data\keyword-audit` | SOURCE_NOT_AVAILABLE | 路径不存在；不得据此导入历史量级。 |
| `D:\Documents\国内GEO系统\geo-open-source-evaluation` | SOURCE_NOT_AVAILABLE | 路径不存在。 |

## 2. 恢复源事实

- `TODAY_NODE_RECOVERY_MATRIX.md` 记录 28 个文件名+内容完整恢复、约 10 个孤立源码、117 个可读 commit 对象但 0/117 根 tree 可展开、120 个有效 blob；
- 两个原 pack 文件签名损坏，PostgreSQL 业务数据/WAL 已确认丢失；
- `TARGET_STATE_MANIFEST.md` 证明 ArticleBrief、人工审核和文章修订封存链有强证据，但大量所有者回忆的 PascalCase 类型名为 0 字面命中；
- 当前 `E:\GEO_RECOVERY_SAFE\bundles\geoplane-*.bundle` 指向灾难后重建早期历史，不是删除前完整仓库；
- 恢复源绝不修改，也不在其中创建索引、临时文件或导出物。

## 3. 百度关键词资产

待核验历史量级为 38 个来源、80,361 条原始观察、71,183 条标准化表达、80,361 条发现、241,083 条需求观察和 3,074 个跨种子共享关键词。当前已检查位置未发现可验证这些数字的 XLSX/CSV、数据库 Dump、Manifest 或历史报告，因此状态为 `UNVERIFIED_ASSET_COUNTS`，不得手工写入数据库。

## 4. 当前本地数据

安全基线 PostgreSQL 只包含脱敏本地演练数据。真实客户数据为 0；Provider 当前运行账本为 0，历史受控测试证据为 1 次。当前数据只能用于验证重建底座，不能当作删除前数据资产。

## 5. 保护点

- `E:\GEO_REBUILD_BACKUPS\geoplane\before-domestic-geo-parallel-recovery-20260719-112446.bundle`
- SHA-256：`5730ad61347c1425027f4fe59b10c0ae42ee4375e4f5d1f794627a5b3626e62f`
- Bundle 已验证包含 `local/closed-pilot-staging-v1@baf550d3fc17589de2b474706560107763176cf1` 的完整历史。
