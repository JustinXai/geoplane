# 当前重建偏差报告

## 总体结论

当前安全基线的账户、Session、PostgreSQL、三角色权限、知识—文章—审核—交付主链、备份恢复和本地运行已经可靠；但它尚未恢复国内 GEO 系统的真实百度需求和国内 AI 平台探测。行业 Pack、账号中心和代理商经营聚合也未闭合。自动信源采集已由最新冻结决定移出主系统，不能再计入当前 P0 缺口。

因此当前偏差严重度为：`HIGH_DOMAIN_DEVIATION_WITH_STABLE_FOUNDATION`。底座稳定，但不能把“通用内容链可运行”宣称为“国内 GEO 系统已恢复”。

## 主要偏差

| 偏差 | 严重度 | 事实 | 纠正路径 |
|---|---|---|---|
| 通用 KeywordQuestionMap 代替百度真实需求运行时 | P0/HIGH | 无导入、来源哈希、原始观察、需求快照、真实量级资产 | Agent B 冻结 Contract、导入与去重语义 |
| Knowledge URL/File ingest 被误当作自动信源系统 | DEFERRED | 自动采集、Atlas、评分和 Citation Analysis 已冻结为未来独立系统 | 主系统仅保留手动上传、URL 登记和人工引用登记；不启动 Source Collection Agent |
| 内容 Provider 被误当作国内 AI 可见性 Probe | P0/HIGH | 无国内平台 Registry、问题集、ProbeRun、RawProbeResult | Agent E 先建 Registry 与 Manual Sample |
| IndustryProfile/VerticalGate 被误当作 VerticalPolicyPack | P0/HIGH | 无 Pack Registry、版本化 Loader、失败关闭策略 | Agent F 复用 Gate 结果，补 Pack Contract |
| 分散代理商读取被误当作经营闭环 | P0/MEDIUM | 无按代理商聚合的待补/待审/待交付/异常状态 | Agent G 按真实业务记录建聚合 Read Model |
| AI 拓词与真实需求未严格分层 | P1/HIGH | 无 generation run、来源快照、AI 标记和默认人工审核 Contract | Agent D 依赖 B Contract 续建 |
| 自动 Source Grounding Gate / Citation Analysis | DEFERRED | 依赖未来独立信源系统 | 本阶段不创建 Migration、不纳入行业 Pack P0 门禁 |
| UI 先于能力矩阵 | P2/MEDIUM | 已产生表现层草稿，但方向已废止 | 草稿封存、不合并；P0 后再做 UI |

## 不属于偏差、直接复用的底座

组织、成员、项目、邀请、签名 Session、CSRF、代理商授权隔离、审计、PostgreSQL、知识包与快照、Opportunity/Human Review、文章 Brief/Draft、质量/平台/行业 Gate 结果、人工 Article Approval、渠道中立发布、PublicationReceipt/Delivery、Provider default-OFF、本地备份恢复。

## 已废止 UI 草稿

- 已停止 UI/Taste/中文页面重建设计；
- 安全提交仅存在于 `local/ui-foundation-v1` 与 `local/client-workspace-cn-v1`，状态为 `SUPERSEDED_NOT_MERGED`（先前提交消息使用更严格标记 `SUPERSEDED_UI_DRAFT_NOT_ACCEPTED`）；
- 不安全 Agency/UI 接线变更封存为 `E:\GEO_REBUILD_BACKUPS\geoplane\superseded-ui-20260719-112446.patch`；
- Patch SHA-256：`55d637922417cafaff3597a07f94bca1dd28536d12c79603662ecc18f4202e0c`；
- 未合并到安全基线、main 或国内 GEO 恢复集成分支。

| UI 草稿分支 | Worktree | 当前状态 |
|---|---|---|
| `local/ui-foundation-v1` | `E:\GEO_REBUILD_WORKSPACE\worktrees\ui-foundation` | 安全检查点 `780a4064732b`，未合并 |
| `local/client-workspace-cn-v1` | `...\ui-client` | 安全检查点 `0d5d62e38c73`，未合并 |
| `local/agency-workspace-cn-v1` | `...\ui-agency` | Typecheck 失败，11 项变更仅保存 Patch |
| `local/ops-workspace-cn-v1` | `...\ui-ops` | 无变更 |
| `local/ui-backend-integration-v1` | `...\ui-integration` | 含后端读取草稿，3 项变更仅保存 Patch |
| `local/ui-supervisor-v1` | `...\ui-supervisor` | 无变更 |
| `local/cn-geo-product-v1` | `...\ui-final` | 无变更；从未接收草稿合并 |

实际被停止并改任恢复事实核对的 UI 子 Agent 为 3 个；隔离的 UI 草稿 Worktree 为 7 个。

## 开放证据缺口

1. 五个指定历史顶层文档和 `.governor` 不存在；
2. 百度关键词 audit 目录、XLSX/CSV、Dump 与历史报告未找到；
3. 开源评估目录未找到；
4. 原仓库 117 个 commit 的根 tree 均损坏，无法恢复完整文件清单；
5. 国内平台 Probe 与 VerticalPolicyPack 的删除前实现证据未找到；自动信源采集已冻结为未来独立系统；
6. 医美 Pack 的具体规则文本/阈值没有可核验证据。

## 决策

第一轮只能判定 `RECOVERY_MATRIX_READY_WITH_OPEN_EVIDENCE_GAPS`。下一步进入 P0 并行能力恢复，不进入 UI，不调用真实 Provider，不导入未验证历史数字。

`Source Collection Status = DEFERRED_SEPARATE_SYSTEM`。自动网页采集、搜索结果采集、Source Atlas、Citation Atlas、全网信源地图、信源评分、爬虫调度、自动提取 AI 回答引用源、自动采集官网和媒体内容、自动化 Citation Analysis 均不属于当前 P0。
