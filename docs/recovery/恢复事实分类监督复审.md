# 恢复事实分类监督复审

审计对象：`local/domestic-geo-reconciliation-v1@75e00c139f9353824958c2adb339a5c2ae5da9a3`

监督基线：`baf550d3fc17589de2b474706560107763176cf1`

最终判定：**`PASS_WITH_CHANGES`**。

## Gate

| Gate | 结果 | 证据 |
|---|---|---|
| `git diff --check baf550d..75e00c1` | PASS | 退出码 0 |
| 生产代码 diff | PASS | 0 文件 |
| Migration diff | PASS | 0 文件 |
| UI diff | PASS | 0 文件；UI 草稿分支不是目标 SHA 祖先 |
| Local Only | PASS（可观察范围） | 目标分支无 upstream；目标差异仅文档；无远端写入证据 |

## 上次 10 项整改

| # | 复审 | 结论 |
|---:|---|---|
| 1 | PASS_WITH_CHANGE | 三层事实来源和 `MISSING_REQUIRED_CAPABILITY` 已修正；顶层六类定义尚未写入 AI 拓词的强制政策例外。 |
| 2 | PASS | 总矩阵已将第 3–9 项收窄为页面/路由/导航片段，并拆出 `VisibilityPage`。 |
| 3 | PASS | 医美 Pack 草案改为 `CURRENT_REBUILD_ADDITION`；AI 拓词按用户强制例外保留 `RECOVERED_SPECIFIED_NOT_COMPLETED`。 |
| 4 | PASS | 活跃文档已移除 `REBUILT_EQUIVALENT（局部）`，信源原语与缺失目标实体分开。 |
| 5 | PASS | 信源报告已改用 recovered-evidence 文档，不再把 owner-restated 架构当恢复工件。 |
| 6 | PASS_WITH_CHANGE | 主矩阵已修正 Gate、Distribution、Receipt；但冻结结构与代理商报告仍有旧分类表述。 |
| 7 | PASS | 已统一为四项核心比例加两个辅助维度，且明确公式/版本仍缺。 |
| 8 | PASS | migrations 现在只证明当前结构，等价性按能力单独核对。 |
| 9 | PASS | 信源模型、Pack Contract、AI Registry/状态机均注明文档级 `CURRENT_REBUILD_ADDITION`，生产能力仍缺。 |
| 10 | PASS | 格式门禁通过。 |

## AI 拓词政策例外

接受用户强制口径：AI 拓词默认分类为 `RECOVERED_SPECIFIED_NOT_COMPLETED`。

相关 AI 拓词报告、Contract、总矩阵和代理商矩阵均已明确：

- 未找到删除前 AI 拓词生产实现；
- 精确字段草案是 `CURRENT_REBUILD_ADDITION`；
- 生产 Contract/runtime 仍为 `MISSING_REQUIRED_CAPABILITY`。

该例外不构成旧实现证据，不要求改成其他分类。

## 必须完成的剩余修改

1. 在《国内GEO系统冻结结构》的六类定义旁加入 AI 拓词强制政策例外，避免与其“必须由恢复源直接证明”的一般定义冲突。
2. 将该文档“已有等价的 Quality/Platform/Vertical Gate”改为：Quality Gate 可按目标语义判等价；当前 Platform/Vertical Gate 为 `CURRENT_REBUILD_ADDITION`。
3. 将《代理商交付能力恢复矩阵》的“交付与回执 = REBUILT_EQUIVALENT”与总矩阵统一为 `CURRENT_REBUILD_ADDITION`，或补充足以证明业务等价的恢复证据。
4. 将冻结结构中“能被历史证据支持的医美规则类别”改为当前目标规则类别；现有恢复源未证明医美规则规格。
5. 对跨文档同一当前对象的 `REBUILT_EQUIVALENT / CURRENT_REBUILD_ADDITION` 差异增加粒度说明或统一分类，重点包括 KnowledgePackage、KeywordQuestionMap、Opportunity 和 HumanReview。

以上均为文档一致性修订，不阻塞已冻结的安全底座，也不授权生产实现、真实数据导入、真实 Probe、UI 恢复或远端操作。完成后可复审升级为 `PASS`。
