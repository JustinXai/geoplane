# P0 代理商业务完整性审计

结论：**PASS**。最终复审对象为 `4d7fdc9`。

## 已完成的业务合同

- 工作流包含客户档案、项目档案、企业知识、账号授权、百度关键词、AI 扩展、用户问题确认、内容任务、内容审核、国内 AI Probe、报告、交付和复盘。
- 状态转换为显式白名单；每次转换要求 actor 与 reason，0015 以只追加事件保存。
- `MARK_READY` 要求报告阶段完成；`REGISTER_DELIVERED` 要求先 READY 且必须提供人工回执引用。
- 读模型已从 PostgreSQL 读取授权客户、项目、知识、关键词审核、内容、账号和交付事实；没有 Fixture 注入，但部分未接事实仍被写成 0，见下方缺口。
- 代理商只能请求自身组织；平台管理员可读取平台代理商聚合。服务层写操作再次调用客户授权 Port。
- 没有计费、佣金、订阅、白标或自动发布状态。

## 已关闭的原阻断

`PgAgencyClientAuthorizationPort`、`PgAgencyDeliveryReadPort`、`PgAgencyDeliveryWritePort` 已实现，运行时可由 `GEO_DATABASE_URL` 生产组合，不再依赖测试注入。

## 最终修复核验

1. 路由比较项目真实 client，服务再次查询 project scope，0017 在数据库层验证 project/client、ACTIVE assignment 和 actor membership。
2. Probe/拒绝未知事实使用 `null`；已批准内容按既有 approval 通过三 Gate 的含义计入 passed。
3. 待交付查询通过 PublishPackage -> ChannelNeutralContentPackage -> DistributionPlan -> Delivery 精确关联。
4. 真实 PostgreSQL 测试覆盖代理商同时获权两个客户时的错配拒绝与正确范围写入；相关 5 文件、12 测试通过。

## 尚待最终 Gate

完成全量测试、类型检查、构建、安全和仓库安全复跑，并在目标数据库验证 0016/0017 已应用。该 Gate 仍在进行，不能预写结果。

代理商业务完整性审计无剩余代码阻断，允许进入最终 Gate。
