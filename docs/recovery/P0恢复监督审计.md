# P0 恢复监督审计

复审对象：集成分支 `local/domestic-geo-p0-v1@59c1545`，增量范围 `352d025..59c1545`。

监督结论：**PASS_WITH_CHANGES**。

原审计的账号秘密、操作员、回执范围和代理商生产组合缺口已在代码层修复；但修复修改了已经应用的 `0010` 文件，违反 Migration checksum 不可变规则，且代理商路由仍可组合不匹配的 client/project，Read Model 仍输出若干无事实支撑的 0。因此维持 `PASS_WITH_CHANGES`。

## 十二项冻结要求

| 检查项 | 判定 | 证据与说明 |
|---|---|---|
| 信源采集未启动 | PASS | 本轮没有新增信源采集运行时、采集 Migration 或自动抓取入口；不得把知识导入称作信源采集。 |
| Provider 不是 Probe | PASS | Probe Registry 仅允许 `MANUAL_SAMPLE`；Probe 路径无 Provider 适配、Endpoint 或网络调用。 |
| AI 词不冒充百度 | PASS | 百度导入只接受 CSV/XLSX 证据并标记 `OBSERVED_DEMAND`；离线拓词合同没有需求指标字段。 |
| 无账号秘密 | PASS | `secretref://` 在服务层和数据库双重校验；API 响应移除引用，明文字段继续拒绝。 |
| 客户账号隔离 | PASS | 账号分配验证操作员成员关系；任务绑定分配；回执在服务层和 DB Trigger 验证同客户、同项目、同账号。 |
| Core 无医美 | PASS | Core 合同只有通用 Pack/Rule/Selection/Evaluation；医美类别仅存在于 `medical-aesthetics-v1.ts` 独立 Pack。 |
| 人工审核 | PASS | 百度关键词有人工审核包与决定记录；AI 拓词默认 `NEEDS_HUMAN_REVIEW`；医美 Pack 强制人工确认。 |
| 无自动发布 | PASS | 代理商交付要求显式 `MARK_READY` 后人工登记回执；未发现自动发布调度器或连接器调用。 |
| 无历史数字伪造 | PASS_WITH_CHANGES | 未嵌入历史 80,361 等数量；但代理商 PG Read Model 将尚未读取的 Probe/内容拒绝事实硬编码为 0，需改成真实查询或“不可用”。 |
| AI 拓词分类 | PASS | 源码明确维持 `RECOVERED_SPECIFIED_NOT_COMPLETED`，没有宣称找回删除前实现。 |
| 未改 main | PASS | `main` 仍为 `cfb230f1565600ae95c2fd1b78ee92086b498095`；安全基线仍为 `baf550d3fc17589de2b474706560107763176cf1`。 |
| 零远程 | PASS（执行证据范围） | 本轮监督未执行 fetch/pull/push/API；可见 reflog 仅有本地 commit/cherry-pick/fast-forward。Git 状态不能证明历史上从未尝试远程操作。 |

## 集成 Gate 记录

- Migration：修复前已知 0015 已应用；`59c1545` 随后直接修改 `0010_domestic_account_center.sql`。迁移器会拒绝已应用文件 checksum 变化，因此现有数据库不会获得新约束，必须改为新增 Migration。
- 测试：修复前最终记录为 `1010 passed / 5 safe skips`；修复后最终复跑仍待完成，不能引用修复前数字证明本次增量。
- Typecheck、Build、Security、Repo Safety：已知集成 Gate 报告通过；最终复跑在监督开始时仍进行中。
- Supervisor 未调用 Provider、真实平台、浏览器自动化或网络。
- `352d025..59c1545` 已新增代理商 PG 适配/生产组合、秘密引用校验、操作员资格、任务/分配/回执范围校验及测试。

## 阻断项

1. 恢复 `0010` 已应用版本的原始内容，将 secret-reference 和 receipt-scope DB 约束放入新 `0016`；在现有集成库执行并验证 checksum 与约束。
2. `workflow` 与 `deliveries` 路由必须校验 body `clientOrganizationId === project.clientOrganizationId`，0015/后续 Migration 也应在数据库层禁止 project/client 错配。
3. Read Model 不得把未查询的 `probeTaskCount`、`failedProbeCount`、`rejectedContentCount` 固定为 0；待交付查询必须按具体 PublishPackage 链路匹配 Delivery，不能以项目任一 Delivery 消除全部待办。
4. 增加真实 PostgreSQL 路由测试并完成修复后全量 Gate。

上述阻断关闭并重跑真实 PostgreSQL 路由测试前，不得输出 `DOMESTIC_GEO_P0_READY`。
