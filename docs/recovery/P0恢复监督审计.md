# P0 恢复监督审计

审计对象：集成分支 `local/domestic-geo-p0-v1@352d025`。Supervisor 工作树保持在 `15f6d68a8519f27b905caf7b285a9587070bed52`，并只读审查了 `15f6d68..352d025`。

监督结论：**PASS_WITH_CHANGES**。

本结论表示六条 P0 工作流的合同、主要状态机和数据表已进入集成分支，冻结边界总体守住；但代理商生产 Read Model 未组合、账号秘密引用缺少格式门禁、发布回执缺少同租户校验，因此尚不能判定 P0 全能力可运行或进入人工产品验收。

## 十二项冻结要求

| 检查项 | 判定 | 证据与说明 |
|---|---|---|
| 信源采集未启动 | PASS | 本轮没有新增信源采集运行时、采集 Migration 或自动抓取入口；不得把知识导入称作信源采集。 |
| Provider 不是 Probe | PASS | Probe Registry 仅允许 `MANUAL_SAMPLE`；Probe 路径无 Provider 适配、Endpoint 或网络调用。 |
| AI 词不冒充百度 | PASS | 百度导入只接受 CSV/XLSX 证据并标记 `OBSERVED_DEMAND`；离线拓词合同没有需求指标字段。 |
| 无账号秘密 | PASS_WITH_CHANGES | API 拒绝常见明文字段且响应移除 `secretReference`；`352d025` 又以 4 个 AI + 12 个内容平台 Registry 限制账号平台注册，但 `secret_reference` 仍接受任意文本，未强制引用格式。 |
| 客户账号隔离 | PASS_WITH_CHANGES | 所有权、授权、项目客户归属与服务层访问控制存在；但操作结果的 `publicationReceiptId` 未验证与任务同客户、同项目。 |
| Core 无医美 | PASS | Core 合同只有通用 Pack/Rule/Selection/Evaluation；医美类别仅存在于 `medical-aesthetics-v1.ts` 独立 Pack。 |
| 人工审核 | PASS | 百度关键词有人工审核包与决定记录；AI 拓词默认 `NEEDS_HUMAN_REVIEW`；医美 Pack 强制人工确认。 |
| 无自动发布 | PASS | 代理商交付要求显式 `MARK_READY` 后人工登记回执；未发现自动发布调度器或连接器调用。 |
| 无历史数字伪造 | PASS | Migration 和测试夹具未嵌入历史 80,361 等不可验证数量；Read Model 只聚合端口事实。 |
| AI 拓词分类 | PASS | 源码明确维持 `RECOVERED_SPECIFIED_NOT_COMPLETED`，没有宣称找回删除前实现。 |
| 未改 main | PASS | `main` 仍为 `cfb230f1565600ae95c2fd1b78ee92086b498095`；安全基线仍为 `baf550d3fc17589de2b474706560107763176cf1`。 |
| 零远程 | PASS（执行证据范围） | 本轮监督未执行 fetch/pull/push/API；可见 reflog 仅有本地 commit/cherry-pick/fast-forward。Git 状态不能证明历史上从未尝试远程操作。 |

## 集成 Gate 记录

- Migration：已知集成 Gate 报告 0015 已应用；Manifest 连续登记 0001–0015。
- 测试：已知集成 Gate 报告 `1009 passed / 5 skipped`。其中恢复证据库保护性跳过 2 项，runtime/canary 未注入导致环境预检跳过 3 项；跳过不能当作通过。
- Typecheck、Build、Security、Repo Safety：已知集成 Gate 报告通过；最终复跑在监督开始时仍进行中。
- Supervisor 未调用 Provider、真实平台、浏览器自动化或网络。
- `15f6d68..352d025` 仅新增账号平台 Registry、注册时平台/账号类型匹配校验及合同测试，没有改变本审计的三个核心阻断。

## 阻断项

1. 为 `AgencyDeliveryReadPort`、`AgencyDeliveryWritePort`、`AgencyClientAuthorizationPort` 提供真实 PostgreSQL 适配，并在生产组合根装配；否则三条代理商 API 运行时不可用。
2. `secretReference` 必须执行引用格式白名单校验，数据库也应增加约束；不得允许任意文本落入引用列。
3. 记录账号操作结果时，必须验证 `publicationReceiptId` 与任务的客户组织、项目一致。
4. 建议验证 `operatorUserId` 属于允许的组织/项目操作范围，避免仅凭用户 UUID 完成跨组织分配。

上述阻断关闭并重跑真实 PostgreSQL 路由测试前，不得输出 `DOMESTIC_GEO_P0_READY`。
